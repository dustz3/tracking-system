{
  "name": "tracking-system",
  "version": "1.0.0",
  "description": "Shipment tracking system with secure API proxy",
  "main": "server/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "npm run build && nodemon dist/server.js",
    "build": "node scripts/build.js",
    "build:watch": "nodemon --watch src --ext pug,styl,js --exec 'npm run build'",
    "deploy:wordpress": "npm run build && node scripts/deploy-wordpress.js",
    "serve": "live-server dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "nodemon": "^3.0.1",
    "pug": "^3.0.3",
    "stylus": "^0.64.0"
  },
  "keywords": [
    "tracking",
    "airtable",
    "api",
    "shipment",
    "logistics"
  ],
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/tracking-system.git"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
